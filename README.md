This repo contains the source for the components of our tool which perform behavioral verification at runtime.  This includes our klee-based interpreter, which executes symbolically executes the client undergoing behavioral verification.

The other supporting components of our toolchain will be supplied at a later date, including build instructions. 
